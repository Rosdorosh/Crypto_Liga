const API_HOST = window.location.host;
const API_PROTOCOL = window.location.protocol;

export const API_URL = `${API_PROTOCOL}//${API_HOST}/api`;
export const ADMIN_API_URL = `${API_PROTOCOL}//${API_HOST}/api/admin`;