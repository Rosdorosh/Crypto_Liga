{
  "name": "crypto-league",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node start-prod.js",
    "build": "cd frontend && npm run build",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\"",
    "tunnel": "ngrok start --config ngrok.yml app"
  },
  "dependencies": {
    "concurrently": "^8.0.1",
    "@web3modal/wagmi": "^3.0.0",
    "wagmi": "^1.4.0",
    "viem": "^1.0.0",
    "ethers": "^5.7.2"
  }
} 